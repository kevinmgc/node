# NODE

Proyecto para aprender Node JS

## Pasos de instalación

- Copiar repositorio de github
- Ejecutar `npm install` para instalar todas las dependencias del proyecto
- Correr `npm run watch` para escuchar cambios en el CSS y generar un archivo compilado en tiempo de desarrollo
- Correr `npm run build` para obtener un build compilado de Tailwind